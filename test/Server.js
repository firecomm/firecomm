var assert = require('assert');
var firecomm = require('./lib/')

describe('Server', () => {
  describe('constructor', () => {
    it('it should work with no arguments', () => {
      

    })
  })

})